import { Component } from '@angular/core';
import { Grid } from "../grid/grid";

@Component({
  selector: 'app-footer',
  imports: [Grid],
  templateUrl: './footer.html',
  styleUrl: './footer.css',
})
export class Footer {

}
